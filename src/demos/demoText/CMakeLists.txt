ADD_EXECUTABLE(demoText demoText.cpp)
TARGET_INCLUDE_DIRECTORIES(demoText PUBLIC ${CMAKE_SOURCE_DIR}/include ${PROJECT_BINARY_DIR}/include ${LUA_INCLUDE_DIR})
TARGET_LINK_LIBRARIES(demoText GLUT::GLUT OpenGL::OpenGL)
INSTALL(TARGETS demoText DESTINATION bin)